<UserControl x:Class="NaNoE.V2.Views.EndView"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
            xmlns:local="clr-namespace:NaNoE.V2"
            xmlns:viewmodels="clr-namespace:NaNoE.V2.ViewModels"
            xmlns:views="clr-namespace:NaNoE.V2.Views"
            xmlns:models="clr-namespace:NaNoE.V2.Models"
            mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    
    <Grid Background="#333">
        <ListView x:Name="lstItems" Background="#333" Margin="3" ItemsSource="{Binding Data}" ItemTemplate="{DynamicResource tmplElements}"
                  HorizontalContentAlignment="Stretch"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled"></ListView>
    </Grid>
</UserControl>
